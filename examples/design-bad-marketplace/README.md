# Bad Marketplace Design Fixture

Usage:

```bash
npx agentproof --path . --design marketplace --profile strict
```

This fixture intentionally misses search, filters, price, location, seller trust, and contact actions.
