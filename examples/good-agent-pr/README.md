# Good Agent PR

This fixture represents the same feature after the agent removes risky code, replaces placeholder copy, and limits its final claim to what can be proven.

The page is intentionally simple: AgentProof should reward boring, auditable work over dramatic but unverified completion claims.
